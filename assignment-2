// Q1. What do you mean by Structures?
Definition:
A structure in C is a user-defined data type that allows you to combine different types of data items together.

#include <stdio.h>
struct Student {
    int id;
    char name[20];
    float marks;
};

int main() {
    struct Student s1 = {1, "Raj", 55.5};
    printf("ID: %d\nName: %s\nMarks: %.2f", s1.id, s1.name, s1.marks);
    return 0;
}
// output:
ID:1
Name:Raj
Marks:55.50

// Q2.What do you mean by Array & Pointer Array?

Array:
An array is a collection of similar data types stored in contiguous memory locations.

Pointer Array:
A pointer array is an array that contains addresses of other variables or arrays.

#include <stdio.h>

int main() {
    // Example of Normal Array
    int numbers[3] = {10, 20, 30};

    printf("Elements of Normal Array:\n");
    for (int i = 0; i < 3; i++) {
        printf("%d ", numbers[i]);
    }

    printf("\n\n");

    // Example of Array of Pointers
    int a = 100, b = 200, c = 300;
    int *ptr[3];   // Array of integer pointers

    ptr[0] = &a;
    ptr[1] = &b;
    ptr[2] = &c;

    printf("Elements using Array of Pointers:\n");
    for (int i = 0; i < 3; i++) {
        printf("%d ", *ptr[i]);  // accessing values using dereferencing
    }

    return 0;
}

Output:
Elements of Normal Array:
10 20 30

Elements using Array of Pointers:
100 200 300

// Q3.Compare Two Structure Variables (Equal & Not Equal)

In C, you cannot directly compare structures using == or !=.
You must compare each member individually.

#include <stdio.h>
#include <string.h>

struct Student {
    int id;
    char name[20];
};

int main() {
    struct Student s1 = {1, "Raj"};
    struct Student s2 = {1, "Raj"};

    if (s1.id == s2.id && strcmp(s1.name, s2.name) == 0)
        printf("Both structures are equal");
    else
        printf("Structures are not equal");

    return 0;
}
 output:
Both structures are erual

// Q4.Perform Arithmetic Operations on Structures

You can perform arithmetic operations on structure members (not directly on the structure itself).

#include <stdio.h>

struct Number {
    int a;
    int b;
};

int main() {
    struct Number n = {10, 5};
    printf("Addition: %d\n", n.a + n.b);
    printf("Subtraction: %d\n", n.a - n.b);
    printf("Multiplication: %d\n", n.a * n.b);
    printf("Division: %d\n", n.a / n.b);
    return 0;
}

output:
Addition: 15
Subtraction: 5
Multiplication: 50
Division: 2
